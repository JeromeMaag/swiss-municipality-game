services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: gemeindeguess
      POSTGRES_USER: gemeindeguess
      POSTGRES_PASSWORD: gemeindeguess
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
